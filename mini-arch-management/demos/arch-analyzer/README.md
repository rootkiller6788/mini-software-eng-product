# arch-analyzer: Analyze Architecture Quality
Reads a module dependency graph, computes Martin metrics (I, A, D'), detects architectural patterns, and identifies violations.
## Run
`cd mini-arch-management && make && ./examples/example_arch.exe`
